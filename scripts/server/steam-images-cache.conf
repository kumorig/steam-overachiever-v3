# Steam images proxy cache configuration
# Place this in /etc/nginx/conf.d/ 
proxy_cache_path /var/cache/nginx/steam_images levels=1:2 keys_zone=steam_images:10m max_size=500m inactive=30d use_temp_path=off;
